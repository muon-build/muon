def _range(i, len, arr)
	if len < i
		arr
	else
		arr = arr + [i]
		_range(i + 1, len, arr)
	endif
end

def range(len)
	_range(0, len, [])
end

def color(c, str)
	'\x1b[3' + c.to_string() + 'm' + str + '\x1b[0m'
end

foreach i : range(7)
	message(color(i, 'hello, world \u2713\n'))
endforeach
