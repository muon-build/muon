# muon analyze complains that this directory does not exist.  I think that is
# good since in real meson.build files there it is highly likely that this
# would be a real error.

assert(false, 'should not have subdir\'d')
