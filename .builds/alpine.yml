image: alpine/latest
packages:
  - curl-dev
  - pkgconf-dev
  - samurai
  - zlib-dev
sources:
  - https://git.sr.ht/~lattis/muon
tasks:
  - build: |
      cd muon
      sh .builds/fullbootstrap.sh
  - test: |
      cd muon
      build/muon test build
