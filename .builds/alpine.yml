image: alpine/latest
packages:
  - curl-dev
  - libarchive-dev
  - pkgconf-dev
  - samurai
sources:
  - https://git.sr.ht/~lattis/muon
tasks:
  - build: |
      cd muon
      .builds/fullbootstrap.sh
  - test: |
      cd muon
      build/muon test -d dots build
