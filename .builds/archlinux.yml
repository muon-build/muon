image: archlinux
packages:
  - samurai
  - clang
sources:
  - https://git.sr.ht/~bl4ckb0ne/boson
tasks:
  - build: |
      cd boson
      mkdir build
      cd build
      CC=clang ../configure
      make
  - ninja: |
      cd boson
      ./build/boson bosonbuild
      samu -C bosonbuild
